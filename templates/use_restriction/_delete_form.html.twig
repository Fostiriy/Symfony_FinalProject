<form method="post" action="{{ path('app_use_restriction_delete', {'id': use_restriction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ use_restriction.id) }}">
    <button class="btn">Delete</button>
</form>
