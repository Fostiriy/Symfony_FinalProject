{% extends 'base.html.twig' %}

{% block title %}ToyCategory{% endblock %}

{% block body %}
    <h1>ToyCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ toy_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ toy_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_toy_category_index') }}">back to list</a>

    <a href="{{ path('app_toy_category_edit', {'id': toy_category.id}) }}">edit</a>

    {{ include('toy_category/_delete_form.html.twig') }}
{% endblock %}
