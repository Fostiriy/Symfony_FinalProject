{% extends 'base.html.twig' %}

{% block title %}Toy{% endblock %}

{% block body %}
    <h1>Toy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ toy.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ toy.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ toy.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_toy_index') }}">back to list</a>

    <a href="{{ path('app_toy_edit', {'id': toy.id}) }}">edit</a>

    {{ include('toy/_delete_form.html.twig') }}
{% endblock %}
