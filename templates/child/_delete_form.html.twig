<form method="post" action="{{ path('app_child_delete', {'id': child.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить этого ребёнка?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ child.id) }}">
    <button class="btn">Удалить из базы</button>
</form>
